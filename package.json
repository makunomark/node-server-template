{
  "name": "np-server",
  "version": "1.0.0",
  "description": "Parse Dashboard and Server configured to work with Nalla.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index.js"
  },
  "author": "Mark Makuno",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "parse-dashboard": "^2.0.5",
    "parse-server": "^4.10.4"
  }
}
